within FMITest;
package PropagateEvents "Library of models to test connected FMUs where events are propagated from one to another FMU"
extends Modelica.Icons.Package;


annotation (preferredView="Info");
end PropagateEvents;
